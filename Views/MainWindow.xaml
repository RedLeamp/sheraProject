<Window x:Class="OfficeManagerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OfficeManagerWPF.ViewModels"
        Title="Office Manager - 공유오피스 관리 시스템" 
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <!-- 버튼 스타일 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F8F8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 카드 스타일 -->
        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Office Manager" 
                       FontSize="28" 
                       FontWeight="Light" 
                       Foreground="#333333"/>
            <TextBlock Text="{Binding SelectedPeriod, StringFormat={}{0:yyyy년 MM월}}" 
                       FontSize="16" 
                       Foreground="#999999"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- 통계 카드 -->
        <StackPanel Grid.Row="1">
            <!-- 총 입금액 -->
            <Border Style="{StaticResource StatCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Background="#E8F5E9" 
                            CornerRadius="8" 
                            Width="48" 
                            Height="48"
                            VerticalAlignment="Center"
                            Margin="0,0,15,0">
                        <TextBlock Text="↓" 
                                   FontSize="24" 
                                   Foreground="#4CAF50" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="총 입금액" 
                                   FontSize="13" 
                                   Foreground="#999999"/>
                        <TextBlock Text="{Binding TotalPayments, StringFormat={}{0:N0}원}" 
                                   FontSize="20" 
                                   FontWeight="Medium" 
                                   Foreground="#333333"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 총 지출액 -->
            <Border Style="{StaticResource StatCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Background="#FFEBEE" 
                            CornerRadius="8" 
                            Width="48" 
                            Height="48"
                            VerticalAlignment="Center"
                            Margin="0,0,15,0">
                        <TextBlock Text="↑" 
                                   FontSize="24" 
                                   Foreground="#F44336" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="총 지출액" 
                                   FontSize="13" 
                                   Foreground="#999999"/>
                        <TextBlock Text="{Binding TotalExpenses, StringFormat={}{0:N0}원}" 
                                   FontSize="20" 
                                   FontWeight="Medium" 
                                   Foreground="#333333"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 순이익 -->
            <Border Style="{StaticResource StatCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Background="#E3F2FD" 
                            CornerRadius="8" 
                            Width="48" 
                            Height="48"
                            VerticalAlignment="Center"
                            Margin="0,0,15,0">
                        <TextBlock Text="₩" 
                                   FontSize="24" 
                                   Foreground="#2196F3" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="순이익" 
                                   FontSize="13" 
                                   Foreground="#999999"/>
                        <TextBlock Text="{Binding NetProfit, StringFormat={}{0:N0}원}" 
                                   FontSize="20" 
                                   FontWeight="Medium" 
                                   Foreground="#333333"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 빠른 정보 -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource StatCard}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="🏢" FontSize="20"/>
                        <TextBlock Text="활성 업체" 
                                   FontSize="12" 
                                   Foreground="#999999"
                                   Margin="0,10,0,5"/>
                        <TextBlock Text="{Binding ActiveCompanies, StringFormat={}{0}개}" 
                                   FontSize="18" 
                                   FontWeight="Medium" 
                                   Foreground="#333333"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource StatCard}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="⚠️" FontSize="20"/>
                        <TextBlock Text="미수금" 
                                   FontSize="12" 
                                   Foreground="#999999"
                                   Margin="0,10,0,5"/>
                        <TextBlock Text="{Binding UnpaidCount, StringFormat={}{0}건}" 
                                   FontSize="18" 
                                   FontWeight="Medium" 
                                   Foreground="#333333"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- 빠른 작업 -->
            <TextBlock Text="빠른 작업" 
                       FontSize="18" 
                       FontWeight="Light" 
                       Foreground="#333333"
                       Margin="0,0,0,15"/>

            <Button Style="{StaticResource ModernButton}" 
                    Command="{Binding OpenCompanyManagementCommand}"
                    Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="🏢" FontSize="20" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock Grid.Column="1" Text="업체 관리" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="›" FontSize="20" Foreground="#999999" VerticalAlignment="Center"/>
                </Grid>
            </Button>

            <Button Style="{StaticResource ModernButton}" 
                    Command="{Binding OpenPaymentExpenseCommand}"
                    Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="💰" FontSize="20" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock Grid.Column="1" Text="입금/지출 관리" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="›" FontSize="20" Foreground="#999999" VerticalAlignment="Center"/>
                </Grid>
            </Button>

            <Button Style="{StaticResource ModernButton}" 
                    Command="{Binding OpenUnpaidManagementCommand}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="📋" FontSize="20" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock Grid.Column="1" Text="미수금 관리" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="›" FontSize="20" Foreground="#999999" VerticalAlignment="Center"/>
                </Grid>
            </Button>
        </StackPanel>
    </Grid>
</Window>
